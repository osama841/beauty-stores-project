<?php

return [

    /*
    |--------------------------------------------------------------------------
    | إعدادات مشاركة الموارد عبر المصادر (CORS)
    |--------------------------------------------------------------------------
    |
    | هنا يمكنك تهيئة إعدادات مشاركة الموارد عبر المصادر أو "CORS".
    | هذا يحدد النطاقات المسموح لها بالوصول إلى أصول HTTP لتطبيقك.
    | يمكنك إعداد قواعد متعددة لمسارات ونطاقات مختلفة.
    |
    | يرجى الاطلاع على: https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS
    |
    */

    'paths' => ['api/*', 'sanctum/csrf-cookie'], // المسارات التي سيتم تطبيق قواعد CORS عليها.
                                                // 'api/*' تعني جميع مسارات الـ API.
                                                // 'sanctum/csrf-cookie' ضروري لـ Laravel Sanctum.

    'allowed_methods' => ['*'], // طرق HTTP المسموح بها (GET, POST, PUT, DELETE, إلخ).
                               // '*' تعني السماح بجميع الطرق.

    // هذا هو الجزء الأهم: تحديد النطاقات (Origins) المسموح لها بالوصول.
    // في بيئة التطوير، يمكنك استخدام '*' للسماح لأي نطاق (لكن هذا ليس آمنًا للإنتاج!).
    // في بيئة الإنتاج، يجب عليك تحديد النطاق الفعلي لتطبيق React الخاص بك.
    'allowed_origins' => ['http://localhost:5173'],   

    'allowed_origins_patterns' => [], // أنماط النطاقات المسموح بها (للاستخدامات المتقدمة).

    'allowed_headers' => ['*'], // رؤوس الطلبات (Headers) المسموح بها.
                               // '*' تعني السماح بجميع الرؤوس.

    'exposed_headers' => [], // رؤوس الاستجابة (Response Headers) التي يمكن للمتصفح الوصول إليها.

    'max_age' => 0, // المدة الزمنية (بالثواني) التي يمكن للمتصفح خلالها تخزين استجابات CORS المسبقة.

    // هذا مهم جدًا لـ Laravel Sanctum:
    // السماح ببيانات الاعتماد (Credentials) مثل الكوكيز (Cookies) ورموز المصادقة (Authentication Tokens).
    'supports_credentials' => true,

];
    // backend/config/cors.php


    